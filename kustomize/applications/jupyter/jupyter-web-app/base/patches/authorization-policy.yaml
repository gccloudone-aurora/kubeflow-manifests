apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: jupyter-web-app
spec:
  action: ALLOW
  rules:
    - from:
        - source:
            principals:
              - cluster.local/ns/ingress-kubeflow/sa/ingress-kubeflow
  selector:
    matchLabels:
      app: jupyter-web-app
